import React from 'react'
import { Feature } from '../@types/typings'

function Feature({
    name, title, image, description, dataCharges, side, backgroundColor, imagePosition, height, imageHeight, security
}: Feature) {
    return (
        <div>

        </div>
    )
}

export default Feature